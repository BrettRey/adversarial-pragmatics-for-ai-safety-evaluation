# Decisions

2026-06-26 — Treat the project as an empirical evaluation paper rather than a conceptual AI-and-language essay. Reason: the strategic value is a reusable safety-evaluation artifact: taxonomy, benchmark, annotation protocol, scoring metrics, and policy/eval memo.

2026-06-26 — Use the short durable project folder `adversarial-pragmatics-for-ai-safety-evaluation` and put the full technical title in the manuscript. Reason: folder names should stay scannable while the paper title can carry the benchmark subtitle.

2026-06-26 — Keep all provided external links in a source-verification queue rather than citing their claims immediately. Reason: organizational agendas, job postings, and safety reports are time-sensitive and must be checked before becoming paper claims.

2026-06-26 — Start the benchmark with harmless payloads and minimal pairs. Reason: the paper needs safety-relevant language-mediated structure without committing operationally harmful content or raw red-team material on day one.

2026-07-01 — Submit the arXiv version as a TeX source archive, not as a generated PDF. Reason: arXiv requires source for TeX-generated work, and the main paper plus supplement can be represented as two ordered top-level TeX files.

2026-07-01 — Make the arXiv source package self-contained with explicit top-level bibliography declarations and bundled open fonts. Reason: arXiv's scanner missed bibliography declarations hidden in the shared preamble, local `.bbl` files were incompatible with TeX Live 2025, and arXiv's workers did not provide the local font names used by the house-style preamble.

2026-07-01 — Use CC BY with primary category `cs.AI`, secondary `cs.CL`, and third category `cs.SE` for the arXiv submission. Reason: the paper is an AI safety/evaluation artifact grounded in computation-and-language methods, with a reusable software/eval infrastructure component.
2026-07-01 — Re-reported judge validation and disclosed pilot circularities after the second review board (reviews/review-board-2026-07-01.md). Added majority-class base rates, Cohen's kappa, and minority-class recall to the judge tables (main §6, supplement); recomputed all values from the artifact confusion matrix, not from reviewer arithmetic. Rewrote §1 box, §6 prose, and supplement conclusion: raw agreement tracks skewed base rates; triage on this pilot belongs to the rule-aided pass; the judge pass is a negative result motivating the validation protocol. Disclosed in §6/§7a/supplement: judge is one of the three evaluated models (18/54 self-judged rows), judge prompt contained the gold expected-behaviour field, and the single adjudicator is the item author. Fixed nested \citep in §1. Reason: seven of eight board reviewers found the raw-agreement reporting misleading against the paper's own cited methodology (Artstein & Poesio); fixing before arXiv announcement avoids v1 carrying the overclaim. Deeper fixes (second adjudicator, item repairs, projection statements) deferred to the development pass / v2.
2026-07-01 — arXiv assigned the permanent identifier `2607.01153` to submission `submit/7776593`. Reason: the paper is now a public preprint, so downstream surfaces should cite `arXiv:2607.01153` and reserve the submission-system id for archive/reference only.

2026-07-10 — Treat *Adversarial Pragmatics* as the flagship job-market artifact, with *Delegation Assurance* and *Evidentiary Assurance* as the programme stack rather than independent proof of a broad technical-AI-safety pivot. Reason: the benchmark and repository demonstrate concrete evaluation design and tooling; the two assurance papers are substantial frameworks but still need implementation, external validation, or reviewer studies before carrying a stronger technical claim.

2026-07-10 — Make the current local manuscript and repository state canonical over arXiv v1 until a public revision is submitted. Reason: arXiv v1 reports the earlier nine-pair / 13/27 strict-pair readout, while the repaired manuscript excludes P008 from strict-pair scoring and reports eight eligible strict pairs plus one diagnostic confidentiality contrast, with 12/24 eligible strict-pair passes.

2026-07-10 — Frame the delegation paper's forward-looking multi-component extension as compositional delegation assurance, not merely multi-agent delegation assurance. Reason: agents, tools, contracts, documents, policies, memories, and judges play different roles in authority preservation; treating them as typed nodes in an authority graph supports the monotonic-authority claim that downstream components may receive narrower but not broader authority.

2026-07-10 — Defer an "authority accounting" paper until after public cleanup and further operationalization of the existing papers. Reason: AI 2040-style macro governance makes the authority-accounting idea real, but the immediate strategic bottleneck is the public artifact mismatch and the need to turn delegation assurance into an executable or testable object rather than adding another conceptual expansion.

2026-07-10 — Freeze the existing pilot as provisional historical data and run independent, role-separated re-adjudication before expanding the benchmark. Reason: the highest-value weakness is author overlap across item writing, expected behaviour, and provisional adjudication; a larger unvalidated item set would not solve it. Study A preserves original labels for comparison and defers failure attribution until task and policy references are independently assessed.

2026-07-10 — Keep naturally occurring ChatGPT, Codex, and Claude Code episodes inside a local-only discovery workflow until Study A has informed the rubric and analysis. Reason: repair turns are useful retrieval evidence but do not prove a uniquely correct original interpretation; raw histories create privacy and third-party-content risk and are neither representative corpus data nor public benchmark items.

2026-07-10 — Classify Study A contributors methodologically as independent evaluators, not as a sampled participant population. Reason: they apply stated linguistic or policy criteria to prompt-response objects; the intended claims concern those objects and the measurement procedure, not contributors' personal reactions or demographics. This is not a claim of automatic institutional exemption: if any institution sponsors, hosts, funds, or conditions access to the work, obtain a written scope determination.

2026-07-10 — Make Study A orientation practice-only, role-specific, and separate from all 54 blind rows. Reason: evaluators need friendly preparation and immediate feedback, but training on real pilot rows or exposed expected behaviour would contaminate independent first-pass ratings. The generated forms retain coarse block timing for burden estimation only, not individual-performance assessment.

2026-07-10 — Conduct Study A initially as a personal independent project rather than seeking an institutional scope determination before outreach. Reason: the current work has no institutional sponsor, host, funding, or access condition, and the immediate need is a small independent evaluation workflow. Revisit the posture and seek a written determination if an institution, funder, or host becomes involved in a way that conditions the work.

2026-07-10 — Use full 54-row coverage per independent evaluator, delivered as three 18-row blocks. Reason: this preserves a clear within-role reference design while allowing evaluators to pause between sittings. Completed partial returns are preserved and explicitly marked as partial; one returned rating cannot establish a stable independent reference on its own.

2026-07-10 — Use an unpaid, bounded volunteer contribution model for Study A. Reason: the project has essentially no research budget and needs a small, transparent ask rather than an implied or inconsistent compensation arrangement. Draft materials must state that no honorarium, authorship, employment benefit, or other material consideration is offered or implied.

2026-07-10 — Retain returned Study A blocks unless the evaluator requests deletion before the stated collection-close and analysis-start cutoff. Reason: this supports the partial-coverage design while preserving a clear deletion window. Before the cutoff, remove requested source files and rebuild local derived outputs; after it, data may already have informed de-identified analysis or aggregate outputs, so deletion is not promised.

2026-07-10 — Use a dedicated Study A email address as the evaluator file-return channel. Reason: for pseudonymous JSON files containing benign study material, a separate MFA-protected address with minimized email content and prompt transfer to local private storage is proportionate to the risk and lower-friction than a dedicated upload system. The mailbox is a transfer point, not the long-term record store.

2026-07-10 — Replace Study A's first-pass safety-risk severity and risk-type fields with visible security-boundary status and type. Reason: outside evaluators cannot validly infer model firewalls, deployment architecture, intended use, or real-world severity from a prompt-response row. The revised form asks only what the visible record establishes; historical pilot severity labels remain frozen and non-comparable to the new fields.

2026-07-13 — Add a comparative test to Delegation Assurance (new §3 subsection after the falsification paragraph): three minimal-pair families that hold capability and provenance fixed while varying authority (indirect injection, confused-deputy disclosure, persistence/subdelegation), scored against a strong baseline of capabilities (Hardy 1988), per-call authorization (ScopeGate, arXiv:2606.28679), and causal attribution of tool calls (AttriGuard, arXiv:2603.10749). Reason: the framework set a falsification bar that the conclusion's "bounded translation" conceded was unmet; the test states where authority status and causal attribution give opposite verdicts (authorized-but-observation-driven; user-driven-but-unauthorized).

2026-07-13 — Cite Effective without warrant (PhilArchive REYEWW) as the conceptual source in the Delegation Assurance introduction, via a new recognition-layer argument: classical access control has no interpretation step, LLM agents reinsert one, and status/recognition/uptake separation is the structure delegation assurance evidences. Reason: the paper previously never cited EwW and lacked a stated motivation for the language-mediated middle layer. EwW itself stays frozen (Synthese package ready; Brett updating the PhilArchive record with the final build, URL stable). Evidentiary Assurance unchanged: its sufficiency test already carries the validity-is-a-separate-showing corollary. Executable harness remains sequenced after Study A.

2026-07-13 — Proposed a six-candidate figure menu for Delegation Assurance (figure-plan.md), anchored on a typed-edge authority-minimal-pair graph for the new comparative-test subsection, with a compositional path figure as the recommended second. Reason: the paper is table-heavy with one figure, and the comparative test's held-fixed/varied structure is graph-shaped; any typed-edge figure is restricted to two edge types (causal, authority) rather than EwW's full typology. Awaiting Brett's trim before building.

2026-07-13 — Built the two surviving figures from figure-plan.md: an authority minimal pair (fig:authority-minimal-pair, comparative-test subsection) and a two-path constraint-preservation contrast (fig:authority-path-pair, compositional section); cut the 2×2, recognition-gap, and procurement-graph candidates and parked the harness outcome matrix. Reason: Brett approved the recommended pair; the two figures share one idiom (typed edges, maroon authority layer) at action and path scale, mirroring the text's structure. Both pass /check-chart-style; build clean at 18 pages.

2026-07-14 — Specify a four-field trace schema for the comparative test (in §3 and the STATUS.md harness action): independently adjudicated authority status with genuine ambiguity admissible as a value, system-assigned status, recognitional fit or misfit, and the action produced through that recognition. Reason: Brett's directive; collapsing the fields hides right-for-wrong-reason and wrong-for-right-reason cases that failure attribution needs, and adjudicated ambiguity aligns the test with the flagship paper's disagreement-as-construct-property commitment.

2026-07-14 — Distribute the four-field schema across the stack: delegation §4's Instruction-status evaluation requirement now scores assigned status against adjudicated status with fit separate from action (aligning the general standard with the §3 test schema); Evidentiary Assurance's sufficiency test gains the record-side corollary (a trace that omits system-assigned status leaves recognition unreviewable); the flagship benchmark's assigned-status/fit rubric axes are parked in STATUS.md as a post-Study-A revision candidate because the pilot instrument is frozen. Reason: fields one and three are review-time constructs, field two is a record-preservation requirement, so the papers split the schema along their existing division of labour; the flagship already has field-one machinery in its two-stage adjudication.

2026-07-14 — Revise the Study A linguistic/task instrument to schema v6 after Brett's first interface self-pilot. Source roles are now a whole-prompt multi-select; the task-giver value is `task_giver_contribution`, not a directive category; multiple clear roles are separated from genuine source-role ambiguity and missing context; task success is kept separate from policy compliance; scope/reference has an explicit not-applicable option; and rationales are optional. Reason: the v5 answers exposed construct conflations that would otherwise turn ordinary multiplicity, quoted material, questions, reference ambiguity, or policy compliance into misleading ratings.

2026-07-14 — Preserve the desktop sticky source panel, top-and-focus reset on row navigation, single narrow-screen flow, safe inline emphasis in help text, and schema/block-specific browser storage in the generated Study A form. Reason: evaluators must be able to inspect the prompt and response while answering every field, move predictably between rows, and resume without stale answers leaking across schema versions or blocks.

2026-07-14 — Use “In confused-deputy failures, efficacy outruns authorization” as the operational hook for the Delegation Assurance comparison, not “authorization failures are cases of efficacy without warrant.” Reason: *Effective without warrant* distinguishes field-valid authorization from moral warrant; an authorization failure does not by itself establish the latter. The full three-pattern differential test belongs in Delegation Assurance, while *Evidentiary Assurance* carries only the record-sufficiency corollary and the under-review Synthese paper remains frozen.

2026-07-14 — Route Alloula et al. 2026 (arXiv:2606.07874, "Safety is Contextual, LLM-Judges Are Not") into the stack: bundle moved to central literature/ with a notes file, bib entry alloula2026safetyContextual, cited in Delegation Assurance §2.5 (steerability failure as authority failure: the judge replaces the delegated policy with its prior), flagship §2 (judge priors limit rubric-aided grading), and flagship §7a (adds a rubric-departure condition to the planned judge-validation experiment). Evidentiary Assurance untouched. Reason: the paper's steerability finding directly supports the stack's judge-validation thesis and sharpens the planned no-rubric/rubric-aided design with a prior-misaligned condition.

2026-07-15 — Close the Study A self-pilot with the schema-v5 sitting standing in for linguistic block 01; no v6 redo. Reason: the v5 block is preserved diagnostic interface evidence and the self-pilot's purpose is usability and burden testing, not labels. Consequences accepted: the v6 timing report stays at 5/6 by design, and the volunteer-workload statement draws on the policy/safety blocks (median ~28 min per 18-row block, single sittings) since the linguistic elapsed times are open-tab wall-clock, not effort time.

2026-07-15 — Audited both Study A v6 forms for wording consistency and presentation order (notes/study-a-schema-v7-form-review.md); no live-form edits. Reason: Brett's none-option flag generalized on inspection: four keys and eight wordings cover three escape meanings, the same none-finding is a first-slot option on one question and a buried NA on the next, escape inventories differ by field, and both forms ask the global judgment before the identification questions it presupposes, against the project's own status-before-licensing doctrine. Timed to the open window: self-pilot closed, no external evaluators approached, so key renames and reorders are still non-breaking.

2026-07-15 — Build Study A schema v7: identification-first question order with the global judgment last on both forms (Brett's call), one standard escape pair (insufficient_visible_context, item_problem) in fixed final slots on every scalar field, substantive first-slot keys for none-findings (no_scope_reference_issue, no_boundary_stated, no_disclosure_or_action_at_stake, no_policy_or_authority_limit), stated-boundary/visible-violation wording split, comma yes/no template, refusal options regrouped, clarification options made ordinal. Reason: the v7 form review found four keys and eight wordings covering three escape meanings, inconsistent none-finding slots, and a question order that asked the global judgment before the identification it presupposes; the window before external recruitment makes key renames non-breaking. Validated end-to-end via the synthetic workflow; self-pilot v5/v6 returns remain local usability evidence, not field-comparable to v7.

2026-07-15 — Narrow the self-pilot label exclusion from "no analysis" to "no ingestion; exploratory-only; provenance-logged". Reason: the harm model is forking paths and adjudicator priming, not looking; both are handled by pre-committing the Study A analysis plan before any external return is opened and by logging when a self-pilot observation informs an item revision or adjudication rule. The ingestion rejection by study ID stands; nothing from the self-pilot enters the independent reference; label-diagnostic outputs live under private/ and are marked exploratory. Findings may become design rationale, never paper evidence. Gate added: tag the frozen analysis plan before opening the first external return.

2026-07-15 — Treat the Study A confidence field as a row-level covariate only, never as confidence in any particular field's answer, and reword its v7 label/help to say so ("How confident are you in this row overall?"). Reason: Brett reports his self-pilot confidence ratings covered the whole row and were generally not driven by the compliance judgment; the old help said "this judgment" (singular) on a five-judgment form, and the v7 reorder placed confidence directly after the global judgment, inviting misattribution. Label diagnostics corrected (column renamed self_row_confidence, README caveat); the earlier chat gloss attaching confidence to specific flips is retracted. Per-field or global-scoped confidence remains an option if a field-attributable signal is wanted, at higher burden.

2026-07-15 — Scope the Study A confidence rating to the form's global judgment (task success; policy compliance) instead of the row as a whole; keys and data shape unchanged. Reason: confidence's analytical job is separating hard-item disagreement from high-confidence criterion conflict at adjudication, which needs field attribution; the v7 escape and ambiguity options already carry categorical uncertainty for every other field, so the one judgment lacking a categorical hedge gets the graded scale at no added burden. Motivated by the self-pilot finding that row-level confidence was generally unrelated to the compliance judgment. schema.md, annotation-protocol.md, and the flagship §5 adjudication sentence aligned; v5-v6 self-pilot confidence marked non-comparable on this field.

2026-07-15 — arXiv package is font-free: no bundled fonts, no fontspec Path=, fonts loaded from TeX Live by bare filename (build_arxiv_bundle.sh). Reason: arXiv strips uploaded font binaries, so the bundled fonts/ + Path=fonts/ approach fails at compile with `The font "EBGaramond-Regular" cannot be found`. All three families (ebgaramond, inconsolata/InconsolataN, charissil) are stock TeX Live packages, so the preamble references them by bare filename and kpathsea resolves the TeX Live copies. Also: both toplevels (main.tex, supplement.tex) must use \input{preamble.tex}, not the repo's \input{.house-style/preamble.tex}; uploading a raw repo toplevel makes arXiv's file scan flag `.house-style/preamble.tex` missing and, because \graphicspath lives in the preamble, cascades to unresolved supplement figures. Verified end-to-end from a clean zero-font extraction: xelatex+biber×3 on both toplevels, main 18pp, supplement 10pp, 0 font errors, 0 undefined citations. Guidance for file-level arXiv replacements: raw repo files safe to upload as-is are sections/*.tex, references-local.bib, figures/*.pdf; main.tex/supplement.tex/preamble.tex must be the build-script (arXiv-flavored) versions.

2026-07-15 — arXiv fonts bundled at package top level (bare filenames, no Path=), superseding the font-free build earlier the same day. Fonts included so the package is self-contained; kept at top level with bare filenames (not a fonts/ subdir with Path=fonts/, the layout that failed on arXiv). Belt-and-suspenders: bundled copies resolve from the build CWD, and if arXiv strips the binaries the same bare filenames fall back to TeX Live's ebgaramond/inconsolata/charissil. Verified from a clean extraction: main 18pp, supplement 10pp, 0 font errors, 0 undefined citations; log confirms the bundled top-level EBGaramond-Regular.otf is loaded.

2026-07-15 — Study A analysis plan is NOT frozen yet; GPT-5.6 pre-freeze review (reviews/study-a-plan-prefreeze-review-2026-07-15.md) returned "not ready to freeze" with 8 blockers. Only blocker 3 independently verified and fixed so far; blockers 1,2,4-8 are reviewer claims still to verify. Reason: freezing today would lock a plan with no confirmatory-analysis contract (blocker 2) and a freeze object (git tag) that does not cover the private row map, package order, comparator snapshots, or judge prompts (blocker 8). Pre-freeze deliverables identified: an estimand table (co-primary vs exploratory) and a hashed manifest + revision ledger, which is also where the multiverse spec (P008 in/out, missingness rules, minority-class definition, item/pair-clustered inference) gets written down.

2026-07-15 — Fixed a confirmed producer↔analyzer bug in scripts/analyze_independent_reassessment.py (blocker 3). The LLM-judge producer (run_llm_judge_validation.py) writes judge_<criterion> columns; the analyzer read bare <criterion>, so real producer output scored 0 eligible rows while reporting availability="available". The synthetic fixture masked it by using bare columns, and build_independent_reassessment.py copies the judge file verbatim, so the two halves were never wired together. Fix: judge_value() reads judge_<criterion> with bare fallback (handles both real and synthetic); build_judge_lookup() raises on duplicate (item_id,model) keys instead of silently overwriting across prompt_variant/run; availability now distinguishes available / no_predictions / not_available. Verified on synthetic (unchanged) and a prefixed-column copy (now identical instead of available/0). Deferred: versioned input schema + semantic crosswalk and the frozen canonical comparator condition, which belong with the estimand-table work.

2026-07-15 — Verified review blockers 1 and 2 against source (blocker 3 already fixed). Blocker 2 (no confirmatory-analysis contract) confirmed: primary_criteria() returns all 10 scalar fields as "primary" while author/judge comparisons quietly use 3, and no plan doc pre-specifies outcomes. Blocker 1 (N-dependent stable reference) confirmed in substance but the reviewer's "N unset" wording corrected: study-protocol-draft.md floors it at ">=2 per role"; what is unset is the assumed N (2=unanimity vs 3=majority in consensus()), max/stopping/replacement, and role-pool disjointness. Drafted benchmark/study-a/analysis-plan.md: a confirmatory/secondary/exploratory estimand table (co-primary = task_success + policy_compliance yield and author-agreement), an explicit multiverse spec (A1 P008, A2 reference rule, A3 missingness, A4 escapes, A5 judge condition), small-N inference discipline (counts not population estimates; cluster by item/pair), and the hashed-manifest freeze object. Marked DRAFT; carries OPEN DECISIONS D1-D5 for Brett; not frozen.

2026-07-15 — Study A analysis-plan decisions settled (benchmark/study-a/analysis-plan.md), D1-D3+D5 and terminology; D4 still open. Terminology: human raters = "evaluators", "judge" reserved for the LLM-as-judge (avoids collision). D1/D2: target 3 evaluators per role (2-of-3 majority sets the reference), close by a date cutoff, fall back to 2-as-unanimity for a short role, flag it; use all valid returns by cutoff, prefer odd N. D3: role pools need not be disjoint people, but the same person must not rate the same row under both roles (priming + independence of the criterion-divergence analysis). D5: co-primary = task_success + policy_compliance; refusal_outcome secondary. D4 (judge comparator) left open pending Brett's Path A (fresh strong open-weights independent local judge, clean prompt) vs Path B (freeze existing glm-compact run as a weak-local-judge secondary result, caveated); frontier API rejected as frozen comparator (non-reproducible/drift). Still before freeze: D4, then build the hashed freeze manifest + revision ledger (blocker 8), then open the first external return.

2026-07-15 — Study A D4 closed: Path A judge comparator = two same-family Mistral judges spanning a capability range, both distinct from the three evaluated models (qwen3:8b, gemma3:12b, glm-4.7-flash), both verified in the Ollama registry. Weak = mistral:7b-instruct-v0.3-q4_K_M (~4.4GB); strong = mistral-small:24b-instruct-2501-q4_K_M (~14GB); both temp 0/seed 1, fit 32GB. Same-family span chosen over cross-family to isolate capability (family held constant), so the claim is "minority-class failure persists as the same family scales 7B->24B." "Most capable model" and frontier API rejected (non-reproducible, stale, confounded). Remaining mechanical steps before the judge comparator is frozen: pull both models; rewrite the judge prompt to drop the expected_behavior field (blocker-5 leak, run_llm_judge_validation.py:354,363); extend runner/analyzer to a named judge file per judge; run each to its own judge_labels.csv. Then build the freeze manifest (blocker 8).

2026-07-15 — Review board (4 independent Opus reviewers: Chen, Liang, Röttger, hostile methodologist; reviews/review-board-20260715-d4-judge-infoset/) resolved the D4 judge information-set decision: Option A (realistic-deployment) — the judge sees prompt + model output + full evaluator codebook only. Unanimous, and backed by two source-verified facts: (1) the human evaluators are taxonomy-blind (study-protocol-draft.md:30), so Option B would break information-state parity with the panel that defines the reference; (2) the four design fields nearly determine expected_behavior (16/18 items), so Option B is a laundered answer key. Corrections forced by the board and verified in code: the plan's sub-step (b) as written ("drop expected_behavior") implemented Option B, not A — true A must also strip the seven taxonomy fields and item_id/variant/model_under_test; and the judge label space is pre-v7 (cannot emit insufficient_visible_context/item_problem/no_policy_or_authority_limit/not_a_refusal), so it must be migrated to v7 before any judge-vs-reference comparison, independent of A/B. The "capability span" framing is softened to a coarse contrast (two q4 same-family models are a convenience pair, not a controlled axis). Option B + old glm run retained only as pre-registered labelled ceiling checks. Open call left to Brett: keep the judge tier as a secondary result vs demote it to a yield-gated appendix (hostile reviewer: ~9 pair-clusters may make the estimate unresolvable). analysis-plan.md updated accordingly.

2026-07-15 — Judge tier kept as a reported secondary result (not demoted). Brett: no point discarding it; the negative result is informative. The review board's power concern (~9 effective pair-clusters) is handled as a mandatory reporting caveat: report reference yield with every judge-vs-reference number, state the effective unit count, frame as small-N secondary not a resolving estimate, and downweight in text if yield is low, never silently drop. analysis-plan.md "Remaining before freeze" updated.

2026-07-15 — Study A judge comparators built and frozen (D4 tasks 1-3 complete). New self-contained runner scripts/run_study_a_judge.py grades the 54 pilot outputs under the Option-A information set (prompt + model output + full v7 codebook only; verified leak-free — no expected_behavior, no seven taxonomy fields, no item_id/variant/model identifiers). Label space read live from schema.json v7 so the judge can emit every reference value including insufficient_visible_context/item_problem/no_policy_or_authority_limit/not_a_refusal; any out-of-v7 value is rejected to a blank field with a note (never a silent wrong label). analyze_independent_reassessment.py extended with a repeatable --judge-labels arg; reports each judge side by side with a judge column (verified on synthetic fixture, backward-compatible). Both judges run deterministically (temp 0, seed 1): benchmark/study-a/judge-comparators/judge-labels-mistral-7b.csv (7B, digest 6577803aa9a0) and judge-labels-mistral-24b.csv (24B, digest 8039dd90c113). 7B: 1 invalid value correctly rejected; 24B: 0 errors. The two judges disagree on 26/162 label cells (~16%), a real coarse capability contrast. Remaining Study A pre-freeze work: build the hashed freeze manifest + revision ledger (blocker 8), then open the first external return.

2026-07-15 — Study A freeze mechanism built (blocker 8), pending Brett's freeze-tag gate. scripts/build_study_a_manifest.py hashes 8 frozen artifacts (analysis-plan.md, schema.json, both judge comparators, and the four analysis-path scripts run_study_a_judge/analyze_independent_reassessment/ingest_independent_reassessment/build_independent_reassessment) plus the two Ollama model digests into benchmark/study-a/FREEZE-MANIFEST.json; --verify recomputes and flags drift (tested: mutating a comparator triggers exit 1 with the hash delta, restore clears it). benchmark/study-a/revision-ledger.md logs post-freeze deviations. This is freeze stamp 1 (analysis path, before external data); stamp 2 adds row_map + blind-package order + author snapshot when the real packages are built. The freeze itself (git tag on the manifest commit) is left as Brett's explicit gate; it is the pre-registration commitment after which opening the first external return is permitted. Note: review blockers 4-7 (missingness rules, judge minority-recall spec, small-N inference, operational blinding) are addressed at the analysis-plan level (multiverse axes A1-A5, inference discipline) but not all independently code-verified; only blockers 1-3 were verified.

2026-07-15 — Fixed review blockers 4-7 (implementation brought to the frozen plan spec) before the freeze tag. analyze_independent_reassessment.py: reference yield now over the fixed presented-row denominator with stable-substantive/escape split (B4); escape values (item_problem, insufficient_visible_context) excluded from author + judge accuracy (B4); judge summary adds majority_baseline/majority_class (B5) and item_macro_accuracy/n_items/n_pairs (B6); minority-class recall adds is_minority (B5). build_independent_reassessment.py: deterministic salt-seeded permutation disperses the model-major source order so 18-row blocks mix models and same-item responses are not adjacent (B7a, verified: blocks mixed, determinism holds); supportPanel() is role-specific instead of task-flavoured for all roles (B7b). Residual documented, non-blocking: single shared shuffle rather than per-rater counterbalanced orders. All verified on the synthetic fixture; analyzer changes backward-compatible. FREEZE-MANIFEST.json regenerated to capture the spec-compliant scripts. All 7 review blockers now verified AND implemented; freeze tag remains Brett's gate.

2026-07-16 — Freeze-readiness review board (6 independent Opus reviewers: Liang, Chen, Röttger, Wallace, Greshake, hostile methodologist; reviews/review-board-20260715-freeze-readiness/SYNTHESIS.md) returned a UNANIMOUS NO-GO on tagging the Study A pre-registration (5 FIX-FIRST, 1 CONDITIONAL). My earlier GO was wrong; the board found real, file-verified defects the D4 board and I missed. Mechanical/integrity fixes done now: (1) removed the FREEZE-MANIFEST git_commit field, which named the parent commit and made the manifest fail its own --verify at that commit (Liang + hostile, verified); (2) added benchmark/items/seed-items.csv (the item set + expected_behavior gold + per-item author labels + item-authoring taxonomy that grounds the Option-B leak argument) to the frozen artifacts — it was absent (Wallace, verified). Manifest now 9 artifacts, verify clean. Deferred to actual freeze time (currently accurate, not fixed now): stripping the plan's "pre-freeze DRAFT / not frozen" text (fix 3), and re-stamping cleanly at the tagged commit (fix 4). STILL BLOCKING, require Brett's design decisions before any tag: (5) co-primary measures trivial no-stakes agreement not the safety/authority discrimination (Greshake+Röttger+Wallace+hostile) — promote authority/source-role + per-pair-flip + policy-at-stake estimands; (6) consensus() can't separate construct contestedness from rater noise (hostile) — needs a disagreement typology and yield≠correctness framing; (7) judge grades all 3 criteria in one pass while humans are role-separated, breaking Option-A parity on the role axis (Chen) — re-run judge in two role-separated passes. Estimand revision (5-7) is the next work.

2026-07-16 — Study A estimand revision approved (Brett, via Roughdraft on notes/study-a-estimand-revision-proposal.md): 5A+5B, 6A, 7A. (5A) add per-pair authority-sensitivity as confirmatory (fraction of the 9 minimal pairs whose independent reference flips in the gold direction) + promote source_role_clarity yield to secondary-confirmatory; (5B) report policy_compliance agreement on the policy-at-stake subset (excluding no_policy_or_authority_limit) with the not-at-stake share alongside, add that value to A4; (6A) split C1-C4 by reference type (unanimous/substantive-majority/escape), add a stable-but-contested estimand, condition recall/candidate-revision on reference robustness, standing statement that yield=convergence not correctness; (7A) re-run the judge in two role-separated passes (linguistic->task_success; policy->policy_compliance+refusal_outcome) to restore Option-A parity, regenerate both comparators. information_flow_action_licensing held at secondary (not co-primary). Execution order: 7A, 6A, 5A+5B, regenerate manifest, then a second (lighter) freeze-readiness pass before the tag.

2026-07-16 — Executed the Study A estimand revision (5A+5B+6A+7A), all verified on the synthetic fixture. 7A: run_study_a_judge.py grades each role in a separate generation (option_a_v7_roleseparated); regenerated both comparators. Role separation moved the 7B policy_compliance distribution sharply (compliant 7->38), confirming the joint-pass cross-role confound; the two judges now differ on 37/162 cells (~23%). 6A: analyzer splits references by type (unanimous/majority-substantive/escape), emits contested-items.csv (rows split across >=2 substantive labels = construct-boundary signal), conditions judge recall on reference robustness (eligible_unanimous/majority_refs); plan carries the "yield=convergence not correctness" standing statement. 5A: analyzer emits authority-sensitivity.csv (per-pair reference-flip rate, all-pairs and excl-P008); source_role_clarity + information_flow_action_licensing promoted to secondary (S0); estimand table adds C5/C6 authority sensitivity. 5B: author comparison reports policy_compliance agreement on the policy-at-stake subset (excl no_policy_or_authority_limit) + not-at-stake share; A4 updated. analysis-plan.md estimand table/multiverse/inference rewritten to match the code. Freeze manifest regenerated (9 artifacts, verify clean). Next: second (lighter) freeze-readiness pass before the tag; plan still marked DRAFT until then.

2026-07-16 — Focused re-verify board (Chen, hostile methodologist, Wallace) after the estimand revision: 2 FREEZE-NOW + 1 narrow FIX-FIRST (Wallace). All resolved their prior defect; no new correctness bugs. Fixes applied: (1) gold-direction match estimand S5 (analyzer joins frozen seed-items.csv gold; reference matches gold on both variants of a gold-differing pair; pre-registered) — computable for task_success (9/9 pairs), disclosed degenerate for policy_compliance (all-compliant gold); (2) author-label-revision-summary now emits the unanimous/majority robustness split the plan commits C3/C4 to; (3) deferred-limitations block added to the plan (prompt-robustness caveat, the compliant<->no_policy_or_authority_limit label-boundary reading of the 37/162 divergence, taxonomy crosswalk, no system tier, zero-variance policy gold). Manifest regenerated (9 artifacts, verify clean). No open scientific defect remains; only the two freeze-time mechanical steps (strip DRAFT text, re-stamp) stand between here and the tag.

2026-07-16 — CORRECTION to the Study A estimand-revision and focused re-verify entries above. S5 gold-direction match was mechanically invalid: frozen seed `task_success_label` values are target-behaviour codes (`output_blue`, `summarize_only`, and similar), whereas Study A `task_success` labels are response outcomes (`success`, `partial`, `failure`, and escapes). Their vocabularies are disjoint, and the synthetic fixture's `SYN-*` IDs concealed the failed seed join, so S5 could never match and has been removed without a crosswalk. C5/C6 also do not identify authority sensitivity because each pair changes both prompt and response; they are retained only as planned-secondary paired panel-outcome divergence, with ordered label transitions and an explicit neither-necessary-nor-sufficient caveat. The unimplemented A2–A4 Cartesian multiverse/specification-curve promise is replaced by a narrow executable reporting contract. Study A terminology is panel-relative, and “confirmatory” is replaced by “planned primary.” The earlier approvals and reviewer reports remain unchanged as history but are superseded by `reviews/study-a-freeze-defect-audit-and-repair-2026-07-16.md`. No freeze tag or opening of external returns is authorized by this correction; the repaired analysis path must pass the verification gate and receive a separate freeze decision.

2026-07-16 — Implemented and independently re-audited the Study A freeze-defect repair. The analyzer now emits panel-relative author/judge agreement, complete unanimous/majority and policy-at-stake numerators and rates, raw+normalized legacy refusal labels, fixed-denominator zero-rating rows, evaluator-role coverage, tie-safe panel-class summaries, and task/policy-only paired outcome divergence with cell-level transitions in frozen item-ID order. The synthetic regression now exercises literal P008 exclusion, the `not_applicable`→`not_a_refusal` crosswalk, tied top classes, missing criteria, exact pair counts/transitions, retired-file cleanup, and the absence of S5/`gold_direction` outputs. The active plan/README/protocol are panel-relative; earlier false claims remain visible under correction banners. Item validation, Python compilation, the end-to-end synthetic workflow, XeLaTeX, house-style, and diff checks pass. Stamp 1 was rebuilt and `--verify` confirms all 10 artifacts, now including `training-items.json`. This is a verified pre-freeze candidate only: no commit, tag, or external-return opening was performed or authorized.

2026-07-16 — CORRECTION after the post-repair five-profile review board (`reviews/review-board-20260716-postrepair-freeze/`): the previous “verified pre-freeze candidate” was not freeze-ready. The shared shuffle did not enforce its no-adjacent-item claim; D3's same-row cross-role rule was incompatible with full-coverage role assignments; boundary-status/type contradictions passed silently; support summaries hid realized \(N\); the production analyzer could run without the frozen 54-key author snapshot and exactly two judge comparators; and the compact outcome-only judge scaffold was incorrectly described as information-state parity with the full human forms. Repairs adopt person-disjoint role pools and a private assignment registry, construct and audit a balanced 18×3 presentation order, bind returns to an opaque package ID, preserve and flag boundary incoherence, report `2/2`, `2/3`, `3/3`, and `other` support patterns, fail closed on production comparison inputs, and retain the two existing judge outputs as non-parity comparators with structural and visible-rule sentinel audits. The current seed set is described as paired/controlled contrasts rather than strict minimal pairs. Judge divergence is 37/162 cells: 21 policy-compliance divergences, including 17 `compliant`↔`no_policy_or_authority_limit` boundary disagreements. Methodological freeze readiness and collection readiness are now separate executable gates. No tag, package distribution, or external-return opening is authorized by this repair.

2026-07-16 — Adversarial implementation audit completed after the five-profile repair. The audit closed additional fail-open paths by binding ingested ratings to the exact attested assignment registry; joining all shared seed, source-output, and author-snapshot metadata; rebuilding and byte-comparing the canonical package; rejecting empty returns, unsafe symlinks, stale or misplaced response stores, and private-metadata aliases; requiring substantive finalized materials and strict evidence schemas; and requiring every manifest-scoped public artifact's bytes and executable mode to match the working tree, stamp-2 manifest, and annotated-tag commit before collection. Mutation tests reproduced each pre-fix bypass and rejected it after repair; unchanged production-like controls continued to pass. A local ignored production package now has a semantically verified stamp-2 candidate, while the collection gate correctly fails because the scoped state is not committed/tagged and operational configuration and assignments are absent. No commit, tag, package distribution, or external-return opening was performed.

2026-07-16 — Reaffirm Study A as an object-centred expert-evaluation design under explicit boundary conditions, and require a written Humber scope determination before recruitment or collection. Reason: the fixed 54 non-human objects, prescriptive codebooks, object-indexed estimands, and instrumental use of agreement support the evaluator role; expertise, pseudonymity, and privacy do not by themselves. The analysis now prohibits evaluator-level, subgroup, timing/burden, confidence-calibration, reasoning-style, and performance outcomes; confidence/rationales are private criterion-specific adjudication metadata, not analysis or publication variables; cross-role patterns are object--criterion combinations. This supersedes the 2026-07-10 decision to proceed without a determination, the 2026-07-10 dedicated-mailbox choice, the 2026-07-15 timing/burden authorization, both 2026-07-15 confidence-analysis descriptions, and the 2026-07-16 statement that evaluator-role coverage was an analyzer output. The collection gate now requires hash-bound sent-request and Humber-response evidence tied to the current plan and protocol, a separate identity-side roster review, and a tag created only after the response is received; it uses a channel-neutral return route and a planning-only 30--40 minute workload estimate, and drops synthetic transfer/timing evidence. Full rationale: `notes/study-a-evaluator-role-justification-2026-07-16.md`.

2026-07-16 — Correct Study A's institutional-posture wording. It is an unfunded project led by a Humber Polytechnic employee, not a personal-independent or unaffiliated project. Brett's exact scope inquiry was sent from his Humber account and asks whether HREB regards the six expert evaluators as human research participants. Earlier personal-independent/no-affiliation wording remains historical and is superseded. This correction does not alter the methodological evaluator-role argument; HREB status remains pending its written response.

2026-07-16 — Supersede the 2026-07-10 fixture-only gate for private naturalistic discovery after Brett explicitly authorized mining his local Codex and Claude Code records. Build a local, provenance-preserving contrast corpus of `likely_pragmatic_failure` and `surprising_success_candidate` episodes under ignored `private/` paths, using interactive human sessions only by default; exclude Codex headless exec, Claude SDK, subagent, meta/task-notification, hidden-reasoning, synthetic/API-error, and inactive-branch material. Reason: naturalistic repair and uptake episodes are useful phenomenon-discovery material, and the contrastive success class reduces failure-only selection. Boundaries retained: retrieval classes are not gold labels; the corpus supports no prevalence or vendor comparison; no raw excerpt or derived benchmark item enters Study A or a public artifact without offline review, controlled reconstruction, and a separate scope decision for claim-bearing use.

2026-07-16 — Replace the raw naturalistic discovery derivative with a privacy-minimized, pseudonymized v2 corpus and a separate owner-only linkage vault. Reason: ignored local storage alone did not deidentify the v1 episode/tool-text derivative, and its static reviewer treated embedded closing-script text as executable structure. Record aggregate before-state inventories and privacy findings; remove full normalized histories, raw episode derivatives, direct provenance, tool arguments/output, correspondence/document content, browser persistence, and active-source files; use keyed HMAC references, field caps, fail-closed withholding, generalized tool metadata, an inert CSP-constrained reviewer, exact final inventories, and independent validation. After v2 passed deterministic and contextual privacy gates, delete both discovered legacy derivatives. V2 remains internal-only and requires new controlled reconstruction before any external use. Exact record: `notes/naturalistic-corpus-privatization-record.md`.

2026-07-16 — Harden the completed naturalistic-corpus remediation after an adversarial code audit found fail-open boundaries not exercised by the first test suite. The final workflow now requires exact corpus/vault file allowlists and canonical metadata vocabularies; strict UTF-8 object-only JSONL; stable source fingerprints; marked direct-child output directories; no-follow, special-entry-safe vault handling; fixed-schema legacy aggregates; atomic owner-only audit output; and a reviewer with no automatic download, clipboard, storage, or network path. Preserve the already-generated candidate/review payload and its generation-builder hash rather than silently implying that later builder code generated it; separately record the hardened future-build hash. Final validation strictly re-parsed all 155 candidate-bearing source files, the reviewer and audit layers were regenerated, and independent code/storage re-audits found no open release blocker. Exact hashes, counts, limitations, and the no-public-use boundary are in `notes/naturalistic-corpus-privatization-record.md`.

2026-07-16 — arXiv v2 replacement (`submit/7830880`, submitted 2026-07-15 UTC) announced public at https://arxiv.org/abs/2607.01153. v2 carries the repaired paired-contrast readout and the top-level font-bundling fix prepared in the 2026-07-16 morning session. Paper password from the announcement recorded in `private/arxiv-credentials.md` (gitignored), not in any tracked file. STATUS.md public-version gap updated: arXiv side closed; repository push, numbered release tag, and DOI archive remain open. No repo commit made from this update, respecting the standing no-commit posture pending the freeze gates.

2026-07-17 — Published an interactive introduction to the paper on the website: https://brettreynolds.ca/papers/adversarial-pragmatics-for-ai-safety-evaluation/walkthrough.html. A guided click-to-reveal tour (five sections: the embedded-command pair, four family contrasts with pilot outcomes, the AP-SEED-016 pass/fail decomposition, judge-vs-base-rate quiz with the validation table, honest-scale summary). All examples and numbers drawn from the v2 manuscript text and sanitized pilot summaries; nothing invented. Linked as a companion page via the mirror generator (paper index + llms.txt), from publications.html ("Interactive intro"), and in sitemap.xml. Rationale: the paper's paired-contrast structure and judge-validation finding are self-demonstrating when the reader makes the calls first; the page doubles as a hiring-legible evaluation-design artifact. Owner: Brett (directed); built by Claude (Fable 5).

2026-07-20 — Treat the automated-vehicle material as a targeted cross-paper case rather than new Study A evidence. The GovTech article served only as a discovery lead; the cited sources are Hinkle's official 2020 prepared testimony and the introduced text and status record for H.R. 7390. Their complete official PDFs are archived in the central literature directory, not Downloads. Hinkle's manufacturer-promise and responsibility position is advocacy, and H.R. 7390 is a proposal rather than current law. Across the papers, keep a manufacturer commitment, a version- and domain-bounded safety argument, authorization of a particular operation, institutional answerability, and liability distinct; likewise, do not treat an incident report or repository as a sufficient action record. Add a fictional, action-indexed controlled-reconstruction hook to Study B while leaving Study A's items, analysis, and collection gate unchanged.

2026-07-21 — Use OpenAI's long-horizon safety report as a bounded first-party operational case across the paper stack. Separate an authorization path (grants, standing, constraints, and handoffs) from a bounded execution trajectory (ordered operations and state changes), and require trajectory authorization to assess the regime's applicable step-local, cumulative, and terminal constraints. Step-local permission doesn't by itself entail trajectory authorization, but an evaluator may not invent a cumulative or endpoint prohibition absent from the declared regime. Replace the earlier unqualified downstream-authority monotonicity formula with grant-relative non-amplification: downstream scope must fall within what the source may validly confer under the upstream grant, not necessarily what the source may personally execute. Treat monitor alerts and pauses as evidence and control events rather than authorization verdicts or standing. Add a harmless static recoverability reconstruction and a static-to-live projection row to Study B without changing its current schema or fixtures; leave Study A byte-for-byte untouched. The source motivates constructs and record fields only: its unnamed model, selected incident cases, missing raw traces and denominators, and unreported monitor/replay metrics don't support prevalence, safeguard-efficacy, or deployment-wide assurance claims.

2026-07-21 — Treat *Adversarial Pragmatics*, *Delegation Assurance*, and *Evidentiary Assurance* as three stand-alone papers joined by typed interfaces, not as one scalar assurance system or three manuscript-dependent chapters. AP owns construct-explicit pragmatic measurement; DA owns regime-relative normative authorization; EA owns evidence-relative review. Every extension beyond observed cases is a prospective projective claim with a named bearer, target, use, warrant, failure rule, and minimum useful reach. Fixture and schema success establish executable coherence only, not target, legal, institutional, or deployment validity. Study A remains unchanged.

2026-07-21 — Close the projectibility-validity revision's executable Stage-5 gate only after permanent regressions reject the AP failed-call, pooled-cancellation, all-failed-invariance, arm-reference, and unbound-production false passes, and after EA's minimum-reach gate requires the declared substantive contrasts rather than row counts alone. Preserve AP, DA, and EA outcome vectors and noncompensation; do not re-enact the AGI-evaluation paper's criticized single-score logic. The next gates are empirical and external, not additional synthetic success: Study B target responses and independent references, DA's three empirical programmes and institutional comparison, and EA's genuine blinded reviewer calibration and forum-specific review.

2026-07-22 — Replace the repository-generic `main.tex` entry point with `adversarial-pragmatics-for-ai-safety-evaluation.tex`, including the generated PDF, build and validation dependencies, and the arXiv package's top-level source name. Retain `delegation-assurance.tex`, `evidentiary-assurance.tex`, and `supplement.tex`, which already identify their roles. Preserve historical records and references to other projects' genuine `main.tex` files. Reason: every active paper artifact should be identifiable without repository context, while historical and external filenames remain facts rather than rename targets.

2026-07-22 — Implemented the HIGH/MED fixes and cheap mechanical LOW fixes from the pairwise cross-section audit (`notes/pairwise-section-audit-2026-07-22.md`) across all three papers; all three rebuild clean with zero undefined refs/citations. Two governing principles for the edits: (a) the machine artifact is the source of truth for any typed enumeration, so the AP §2/§3 failure-attribution lists were aligned to the `benchmark/rubrics/taxonomy.md` codebook labels rather than to each other; (b) where a set is counted ("ten primitives", "seven legal families", "nine layers"), align the outlier to the counted set instead of expanding the count, to avoid creating new enumeration drift — hence EA's context requirement was folded into the existing Attribution primitive (matching the crosswalk) and EA §4's ad-hoc layer list now cross-references the canonical nine-layer table rather than restating it. Added an A→C citation and a one-sentence three-paper program frame (AP = measurement layer, DA = normative-authorization layer, EA = record-warrant layer) in AP §8; added the Goodman/projectibility first-mention gloss to DA (previously absent). Deferred: the softer redundancy trims (DA answerability-caveat triplication, EA projective-declaration/incident-bundle triplications) and the TikZ figure-rail relabels, which need authorial voice decisions. Open question flagged to Brett: AP §3 dropped "omitted information" from the transcript list because it is not a codebook label — decide whether to add an `omitted_information` failure-attribution label instead.

2026-07-22 — Added an `omitted_information` failure-attribution label (Brett's call), reversing the earlier default of dropping the term. Omission (withholding relevant information without asserting anything false) is treated as distinct from `misreporting` (an actively false or misleading self-report). Added consistently to every place the inventory lives: the codebook (`benchmark/rubrics/taxonomy.md` label list, disambiguating gloss, and the agent-transcript diagnostic question), the validator (`scripts/validate_items.py`), the adjudication review app (`scripts/build_adjudication_review_app.py`), rater training (`benchmark/rubrics/rater-training.md`), and the AP §2/§3 prose lists (both, keeping their membership matched). Left untouched: the historical LLM-judge prompt's deliberately smaller label subset and all frozen pilot outputs. `validate_items.py` still reports `ok` on the seed CSV; no existing item uses the new label.

2026-07-22 — Major revision of the Adversarial Pragmatics paper in response to an external review recommending major revision. Verified the review's claims against source before acting; its sharpest empirical claim was refuted. The reviewer argued AP-SEED-009's models converged on GREEN because Instruction B is linearly previous, making the reference defective. The pilot rows show the opposite: glm-4.7-flash and qwen3 output BLUE (resolving to Instruction A, qwen3 explicitly), gemma3 output GREEN. The three-way split is stronger evidence of genuine referential underdetermination than the reviewer's mechanism, and it was previously unreported. The reference was NOT revised: the expected behaviour was fixed before outputs were inspected, and amending it now would be the output-dependent revision the protocol forbids, so AP-SEED-009 is routed to Study A output-blind adjudication as a reference diagnostic with its capability_failure attribution re-opened. Also found from the seed CSV that ALL NINE pairs change the required response act alongside the designated dimension, not "several" as previously written; the pilot statistic is renamed joint pair completion accordingly, and a full 18-item inventory table now makes this inspectable. Added item-clustered bootstrap intervals (10,000 resamples of the 18 items, scripts/judge_agreement_intervals.py): the recomputation reproduces Table 2's published figures exactly, and four of six chance-corrected intervals include or touch zero, so the judge is indistinguishable from a constant labeller on four label families. Other changes: three-axis presentation of the taxonomy (the axes already exist in the schema, so this is presentational, not a data restructure); functional definitions of "pragmatics" and "adversarial" plus an English/chat-platform scope declaration; cross-criterion divergence replacing "criterion conflict" for AP-SEED-016; a four-level failure signature/locus/attribution/evidential-status scheme adopted for Study A; a seven-column comparator table against SEP, AgentDojo, IHEval, BIPIA, InjecAgent, and Tensor Trust (IHEval and InjecAgent added to references-local.bib, both independently verified against arXiv and ACL Anthology; Tensor Trust already present as toyer2023tensor); Study B gate specification moved to the supplement leaving one illustrative gate; Figure 4 replaced by a compact table. Deliberately NOT done, pending Brett: retitling, releasing raw model outputs, a Zenodo DOI/release tag, and any use of the staged mistral-7b/24b disjoint-judge comparators, which remain Study A material under a checkpoint marked candidate_pending_explicit_tag.

2026-07-22 (later) — Four authorized changes completed. (1) Retitled to "Adversarial Pragmatics for AI Safety Evaluation: A Diagnostic Framework and Seed Benchmark for Language-Mediated Control". The main clause is kept because it matches the repository name, the source filename, and the posted preprint identity; only the subtitle changed, to stop overclaiming benchmark maturity while keeping the empirical framing the project brief requires. Applied across 12 files including CITATION.cff, LICENSE.md, submission/arxiv/metadata.md, and critically the `reynolds2026adversarialPragmatics` entry in references-local.bib, which both sibling papers cite. NOTE: arXiv 2607.01153 still carries the old title, so a v2 posting is needed before the sibling citations resolve correctly. (2) Used the mistral-7b/24b disjoint judge comparators, but NOT via naive agreement. They use the Study A schema vocabulary, not the pilot's (`not_a_refusal` vs `not_applicable`, plus `no_policy_or_authority_limit` and `insufficient_visible_context`, which have no pilot equivalent); a naive comparison produced a spurious 3.7% refusal agreement that is purely a vocabulary artifact. Rather than impose a post hoc crosswalk, the reported comparison is restricted to the two labels all three vocabularies share. Result: no judge recovers even half the seven noncompliant rows, the most favourable condition (rubric-aided, self-grading) does best at 3/7 against 1/7 and 2/7, partial task success is recovered 0, 1, and 0 times out of 11, and mistral-small:24b never emits `partial` across 54 rows. The minority-class failure is therefore not an artifact of the circular glm condition. The "No Study A results are reported" claim was amended accordingly. (3) Released the raw model outputs, run metadata, and the per-row judge labels (the last because the interval script cited in Table 2's caption would otherwise not be reproducible from the repo); scanned for secrets, emails, and local paths first, all clean. Browser adjudication exports remain excluded as private adjudication metadata, consistent with the rater protocol. (4) Added a TikZ evidence-chain figure in Section 1 running stipulated regime to projected target, with each link labelled by what it requires; rendered and visually checked.

2026-07-22 — Venue decision: *Minds and Machines* for *Delegation Assurance for AI Systems*. Owner: Brett; agent-assisted (Claude Opus 4.8, two background sweeps of 28 journals, all fit-evidence DOIs re-resolved against Crossref). Record: `submission/venue-decision-2026-07-22.md`. Journal-reader contract: the paper changes the debate about what an AI audit record establishes, by showing that a complete and internally consistent trace of a machine action is compatible with that action being unauthorized while a valid authorization can leave no trace at all. Constraint fixed by Brett: journals only, no conferences (no conference funding), which removed SaTML, AIES, FAccT, NeurIPS D&B, CCS, S&P, and USENIX. Chosen over Journal of Computer Security, which the agent initially preferred for its "meaning and implications of security" scope clause and its live authorization-formalization neighbours; Brett's counter-argument is recorded in the decision record and accepted: the paper has no threat model, attacks, defences, or security evaluation, so submitting to a venue whose methodological norm is formal access-control modelling with proofs would repeat the topic-match-over-reader-match error the outcomes ledger documents five times. Also considered and rejected: Journal of Responsible Technology (best neighbour article found anywhere, Pittman & Schaefer 2025 on authorization to operate, but a 10,000-word cap against 11,800 before the referee's additions); AI Magazine (article type is literally "conceptual analyses" and it is free OA, but 6,000–9,000 words and "without complex equations or formulas"); Artificial Intelligence and Law (scope names deontic notions and normative systems, but six-day median first decision and the paper does not model legal reasoning); AI and Ethics (fails its in-scope gate, which requires a defended claim about what is right, fair, harmful, or just). Disqualified on written policy: Computers & Security (AI/ML system security explicitly out of scope), Internet Policy Review (prohibition-model AI policy), AI & Society (LLM use strongly discouraged), ACM TOPS/TIST and IEEE S&P (no non-empirical route). Risk accepted: the absent authorization literature (ABLP, SPKI/SDSI, Delegation Logic, SecPAL, UCON, XACML) costs less at M&M because those reviewers do not own that canon, but an acceptance would not retire the defect. Three prerequisites before package work: rewrite page one for a philosophy readership, resolve the title's *typed authorization semantics* overclaim against §3.5's own concession, and link the public artifact. Consequence: M&M is now spoken for, so the AGI evaluation paper and Truth-Tracking Profiles need their venue decisions made against that.

2026-07-22 — Implement the evidentiary-assurance major-revision punch list against an AI-governance journal target (Brett's call), rejecting the reviewer's proposal to split the projectibility material into a companion paper. The action-level/program-level separation is the spine of the argument; without it the paper reads as another assurance checklist. The reviewer's underlying complaint (the test arrives halfway through) was answered by reordering within sections and by opening the introduction on a concrete contested action, not by amputation. Adopted in full: entity-indexed `J_B(z)` and `J_C(f)` with a single-entity conjunction requirement; a fifth node status `not_established` separating an unevaluable record from an adequate record whose showing falls short; rebutting/undercutting defeater semantics with undercutters barred from yielding substantive defeat; `E_action`/`E_review`/`E_external` typing; node-specific evidential standards and burdens; an applicability-map governance subsection; the assurance-case, audit-evidence, and accountability literatures; Figure 1 renamed a typed review architecture; RFC 6962 replaced by RFC 9162 in both papers; `J_C` renamed object-forum capacity with claimant-side access declared out of scope; artifact availability, methods, and limitations sections; abstract rewrite.

2026-07-22 — Entity-index the bearer and forum verdict families in the executable artifact, not only in the prose. The external review inferred the "Frankenstein bearer" risk from the PDF; inspection of `reviewer-response.schema.json` confirmed it was live in code, since `node_verdicts` carried no entity field and all sixteen fixtures held exactly one bearer and one forum, so the failure was unexercised. Verdicts are now addressed by `(node, entity)`; the reviewer response carries a `conjunction_resolution` per family; the validator rejects a resolution that doesn't follow from the per-entity vectors; and the analyzer tracks `entity_conjunction_error_rate` at zero tolerance, because assembling a family from several candidates is a logical error rather than a judgement call. Four fixtures added (20 total): EA-MC-019/020 split bearer and split forum, and the EA-PAIR-06 gap/insufficiency pair isolated at `A_DELEGABILITY` with completeness held fixed.

2026-07-22 — Canonicalize the evidentiary artifact JSON and add `scripts/stamp_evidentiary_artifacts.py` to recompute the binding graph, rather than maintaining hashes by hand. Re-stamping is legitimate only pre-review, so the script refuses when any genuine reviewer response exists, when an unblinding record exists, or when the declared boundary has passed; `--check` makes it a CI guard and it is wired into `assurance-check`. Version bumps recorded rather than silent in-place edits: claim graph and map to 3.0.0, reference keys to 4.0.0, records to 2.0.0. Guard behaviour is covered by `scripts/test_stamp_evidentiary_artifacts.py`, which runs entirely against a temporary directory. Deliberately not tested by writing a `genuine_reviewer` file into the artifact tree: the repository's standing claim is that no reviewer responses exist, and a fabricated one must never be created there even transiently.

2026-07-22 — Reaimed Delegation Assurance's opening at the *Minds and Machines* readership, after reading the venue's recent corpus. Correction first: the venue sweep's claim that M&M had published nothing since 2025 on agent authorization or delegation was a query artifact (an eight-term `query.bibliographic` string acting as a relevance ranker, returning two items). Pulling the journal's full 2025+ output, 86 articles, shows a dense current conversation. The "opening a thread, not joining one" risk recorded in `submission/venue-decision-2026-07-22.md` is withdrawn on that evidence. Sources read, not summarized: Rashid 2026 (M&M 36:30) on the threshold where a model output becomes an executable intervention, which poses this paper's question and explicitly leaves it open; Nguyen et al. 2026 (M&M 36:29) distinguishing causal from normative controllability; Santoni de Sio & van den Hoven 2018 on meaningful human control (tracking and tracing conditions); Sterz et al. 2024 on effective oversight (causal power, epistemic access, self-control, fitting intentions ~-- note "fitting", not Rashid's paraphrase "role-appropriate", checked against the source); Novelli, Taddeo & Floridi 2024 on accountability (context, range, agent, forum, standard, process, implications). All eight relevant open-access M&M papers plus the three canon papers are archived in the central literature directory. The positioning claim that resulted, and that the new §1 and §2.3 make: this literature specifies conditions on *persons* and none on *authority*. An overseer can satisfy every Sterz condition while lacking standing to amend the mandate, and an operation can be validly authorized while nobody in the chain satisfies them. `Stand_R(x,o,q,t)` is the missing layer. §1 no longer opens on prompt injection; the Dams Safety NSW genealogy is demoted to a footnote; the trajectory non-entailment result is promoted to page one. §2 retitled from "What Existing Agent-Security Work Leaves Open" to cover both literatures, with a new §2.3. Five verified bib entries added to `references-local.bib`. Build clean, zero undefined citations, style checker clean, 37 pp. Still open: the *typed authorization semantics* title overclaim, and the missing artifact link.

2026-07-22 (evening) — Ran a fresh three-by-two judge experiment instead of relying on the staged Study A comparators, at Brett's prompting, and it overturned the conclusion drawn from those comparators. The staged files were unusable for a like-for-like comparison because they changed model, scaffold, criteria set, and label vocabulary at once. The new design holds the pilot's own judge prompt fixed and varies exactly two things: judge model (glm-4.7-flash from the evaluated set; mistral:7b and mistral-small:24b disjoint from it) and rubric visibility. A new `compact_no_rubric` prompt variant was added to scripts/run_llm_judge_validation.py; it is identical to `compact` except the item's expected-behaviour field is withheld, making rubric visibility a single-factor manipulation. All five new cells returned 54 rows with zero parse errors. Findings: (1) the earlier claim that disjoint judges do worse is FALSE. The disjoint mistral-small:24b with the rubric is the strongest cell, recovering 6/7 noncompliant rows against 3/7 for the self-grading judge, so circularity was not what produced the original negative result. (2) The rubric effect is directionally consistent but only one difference survives a paired item-clustered bootstrap: mistral-small:24b policy compliance, +16.7 points, 95% CI 1.9 to 33.3. The general rubric claim is therefore NOT made. (3) The finding that survives everything: no cell recovers partial task success (0, 0, 2, 1, 0, 0 of 11), and mistral-small:24b achieves the only above-base-rate task-success agreement while emitting `partial` zero times in 54 rows, so its aggregate advantage comes from collapsing a three-way distinction into a binary. Aggregate agreement and minority recovery come apart, which is the paper's own thesis demonstrated on its own measurement route. The falsified claims were removed from the abstract, Section 6, and the limitations section. All six runs' per-row labels are released. Caution recorded for future work: run_llm_judge_validation.py overwrites the shared benchmark/results/summaries/ judge-validation files on every invocation, including from a 2-row smoke test; use --summary-dir to redirect, or the released Table 2 sources are clobbered.

2026-07-22 (second review round) — Implemented the minor-revision punch list. The reviewer caught a genuine error of mine: the abstract and Section 6 both claimed "no configuration recovers partial task success" while Table 8 in the same section reported 2/11 and 1/11 for the two mistral:7b cells. Corrected to "no cell recovered more than two, and four recovered none." Two neighbouring claims were also too causal and were withdrawn: model identity, parameter count, instruction-following ability, and independence from the evaluated set all vary together across three models, so the experiment shows only that the failure profile isn't confined to the circular self-grading condition, not that capability matters more than independence or that circularity contributed nothing. Renamed the judge conditions from "rubric visible/hidden" to "expected behaviour visible/withheld", since the general rubric (label inventory and output schema) is constant across both conditions and only the item's own expected-behaviour field varies. Quarantined P005 from the quantitative summaries: joint pair completion is now reported as 12/24 under the frozen historical references and 12/21 with the reference-review cells set aside (numerator unchanged, since P005 contributed no passes), the pair-accuracy figure caption marks P005 as under reference review rather than three model failures, and the judge's failure-attribution figure carries a sensitivity analysis (6/18 falls to 4/15 when the three contested capability_failure rows are excluded, removing two agreements on a label the pilot no longer stands behind). Weakened "direct evidence that the reference is underdetermined" to "direct evidence that the item elicits competing antecedent resolutions", since the regime-relative reference is fixed by the declared adjudication procedure rather than by model behaviour. Aligned the abstract with the introduction ("primarily methodological, supported by...") and stated there that judge comparisons are against provisional author labels. Restructured the taxonomy-axes table to a "partly recorded by / not yet represented" form, because source_role and authority_level do not encode policy-boundary status or information-flow authorization and context_source does not encode trajectory horizon. Stated the status of the Study B thresholds honestly as provisional preregistration choices with no sensitivity analysis run, rather than inventing a justification that isn't recorded anywhere in the repo, and committed the construction pilot to reporting each cell at nearby thresholds. Added a per-cell evidence map for the benchmark comparison to the supplement, which required giving the supplement its own bibliography since it previously had no citations. Changed "adjudicated pilot" to "author-labelled pilot" in three captions to protect the distinction between first-pass labelling and second-stage adjudication. Fixed the page-11 float that split a paragraph. Not done, pending Brett: a frozen release tag, commit hash, and archival DOI.

2026-07-22 — Second-round referee response on Delegation Assurance. Two sentences added earlier the same day were withdrawn as wrong, not merely broad. (1) "The available answers specify conditions on people" asserted the paper's central gap over a literature that fills it: specifying conditions on authority is what SPKI, SecPAL, UCON, and XACML do. Now "the prominent answers in AI governance." (2) "Per-call authorization, the level at which current mechanisms operate, can't see this" was an unevidenced negative over an unsearched space, contrary to the portfolio's own rule; the reviewer's UCON counterexample couldn't be verified directly (ACM DL is Cloudflare-blocked and Semantic Scholar carries no abstract), but the sentence wasn't defensible either way. Now hedged to "many current agent mechanisms operate per call, and without additional stateful control they don't detect cumulative or terminal violations." The reviewer's third softening, "This paper answers that question" to "develops a framework for answering," was declined: the replacement is limp and a framework paper answers in the only sense available to it. Also fixed: the Alloula author order, reversed since round one (arXiv order is Alloula, Licini, Batchkala, Goldfarb-Tarrant). Added `sections-delegation/08-code-data-availability.tex` naming the repository, artifact commit `95e927b`, the three versioned schemas (regime-2.0.0, authorization-trace-2.0.0, oracle-2.0.0), seven fixtures, two analyzers, and the hidden-oracle construction, with an explicit statement that oracles authored against fixtures are weaker than a differential implementation. Defined `typed` in §1 as partition by bearer, inferential role, and machine-readable schema, explicitly not a static type system or proof calculus, and stated that the formal contribution is a meta-model rather than a complete authorization language. That takes the reviewer's option 2 over a retitle. Venue note: the reviewer ranks the missing formal-authorization literature first and says it is decisive "for a security, access-control, or formal-methods venue" while the paper is "much closer to publishable" for an interdisciplinary AI-governance venue, which is the M&M reasoning; that reading is therefore demoted to a defensive citation rather than a blocker. Still open and unchanged from round one: non-amplification as scope inclusion rather than a history-sensitive relation, institutional mode as an adjudication interface with a worked contested case, the fifth content-interpretation field, and the frozen statistical thresholds. Newly open: `references-local.bib` gives Reynolds 2026a the retitled form while arXiv:2607.01153 still carries "A Benchmark for Instruction Conflict, Embedded Commands, and Policy Ambiguity" (verified against arXiv metadata); either the public record or the bib has to move. Build clean, zero undefined citations, style checker clean, 37 to 38 pp.

2026-07-22 (Gelman pass) — Went past softening the statistics to doing the analysis Gelman would do. The judge experiment's one eye-catching result, mistral-small:24b losing 16.7 points of policy-compliance agreement when the expected-behaviour field is withheld, is the largest of three noisy per-judge effects on 18 clustered items: a textbook Type-M setup where the largest observed effect overstates the truth. Rather than test whether it clears zero, fit Gelman's eight-schools normal-normal hierarchical model to the three per-judge effects (scripts/rubric_effect_partial_pooling.py): d_j ~ N(theta_j, se_j^2) with item-cluster bootstrap se_j, theta_j ~ N(mu, tau^2), flat prior on mu, half-normal(0, 15 points) weakly informative prior on tau, posterior computed exactly by quadrature over tau so it's deterministic. Partial pooling shrinks the 16.7-point effect to 9.7 [-1.8, 25.7] and puts the across-judge mean at 5.2 [-8.5, 21.6]; both now include zero, and the shrinkage is robust to halving or doubling the prior scale (8.6 to 10.6 for 24b across scales 10/15/25). The precise self-grading effect barely moves; only the imprecise outlier shrinks, which is the pooling working. Reframed the whole judge section away from the include/exclude-zero hinge (which is what McShane-Gelman "abandon statistical significance" argues against) toward estimates, magnitude, and uncertainty; added a few-clusters caveat that an 18-cluster nonparametric bootstrap undercovers, so the intervals are if anything too narrow and the conservative reading is safe. Softened the abstract's "indistinguishable from a constant labeller" to a statement about what 18 clusters can resolve. Cited gelman2014types (Type S/M) and GelmanBDA3 (eight schools), both already verified in the central bib. All reported numbers come from the committed script. The paper's load-bearing claim, that minority-class collapse persists across all six cells, never depended on the rubric effect and is unchanged; the rubric sub-result is now appropriately humble.

2026-07-22 — Gelman-facing statistical revision of Delegation Assurance §3.6 (the three comparative programmes), in response to a second-round review and Brett's instruction to make it a paper Gelman would be proud of. Gelman's order is simulate first, then write prose around the numbers, so a design analysis by simulation was built and run before any prose changed: `scripts/design_analysis.py` (regression tests `scripts/test_design_analysis.py`, Makefile target `design-analysis`, outputs under `assurance/delegation/empirical/design-analysis/`). It generates data under declared effects and clustering structure, applies the prescribed estimators, and reports sign/magnitude errors and coverage (Gelman & Carlin 2014). Three findings, each indicting one frozen v1 construct and naming its replacement: (1) local discrimination — subtracting the largest of three matched nuisance sensitivities over-subtracts (max of noisy nonnegative quantities is inflated), biasing the family mean low by ~26% of a 0.15 effect, where modelling the common nuisance mean is nearly unbiased; and gating on a one-sided LCB>0 exaggerates the passing estimate (Type M ~1.12× at 30 pairs). Fix: model nuisances as covariates, partially pool across families, report estimate+interval, tie go/no-go to declared losses. (2) predictive ablation — ten-bin ECE at n=30 false-fails a perfectly calibrated predictor ~98% of the time (mean ECE ~0.18) from binning noise; and the z cluster interval over 10 families undercovers (~0.92) where a t/multilevel interval reaches nominal (~0.95). Fix: retire ECE for calibration slope/intercept or a curve; use the t/multilevel interval; Brier kept as a proper score. (3) reviewer reconstruction — "the larger of case-cluster and reviewer-cluster SE" undercovers (~0.87) because it drops a variance component; a crossed variance recovers nominal (~0.95) at the same 6 reviewers × 10 cases, so the estimator, not the reviewer count, is binding. Fix: cross-classified reviewer×case model. Multiplicity is handled by partial pooling across families (Gelman, Hill & Yajima 2012; Gelman & Hill 2007) rather than four independent tests; prospective declaration constrains but doesn't remove discretion (Gelman & Loken 2014). Four Gelman citations verified against Crossref and added. Scope decision: the §3.6 prose, the run design analysis, and the citations are the Gelman-proud deliverable and are done. The frozen v1 `program-specifications.json` and the 838-line `analyze_delegation_programs.py` still implement the LCB-gate/ECE verdict logic; rather than half-edit them (which broke schema conformance and 11 analyzer tests on a first attempt), they're kept as the frozen v1 record, and §3.6 and the availability section state plainly that those estimators supersede v1 and that reimplementing the analyzer to compute estimate-and-interval is a declared next step. That keeps the artifact honest (no claim the analyzer already implements multilevel estimators) and the tree green. Build clean, 0 undefined citations, 38→39 pp, style clean (2 pre-existing false positives), all delegation validators and both test suites pass. Remaining, for Brett: whether to reimplement the analyzer now (large) or leave it as the superseded v1 record.

2026-07-22 — Reimplemented the delegation empirical analyzer for repo coherence, dispatched to gpt-5.6-sol via `codex exec` (Brett's call: "use GPT sol"). The v1 analyzer implemented the retired LCB-gate/threshold/ten-bin-ECE/max-of-two-SEs logic that §3.6 now disavows; leaving it would recreate the paper-artifact mismatch the referee flagged. Codex was given a surgical brief scoped to four files (analyzer, its tests, program-specifications.json, its schema), with the variance-component decision made up front (method-of-moments/ANOVA, numpy+scipy only, no new deps) so it wouldn't drift. It stayed exactly in scope. Verified independently, not on its paste: retired constructs gone (0 grep hits for normal_lcb/1.96/LCB-strictly, ten-bin-ECE gate, max-of-two-SEs); new estimators present and correct on inspection — local uses the modelled common nuisance mean (not the max) with normal-normal empirical-Bayes shrinkage (shrinkage_weight = between/(between+within); a test asserts pooled_grand < pooled_family < unpooled_family); predictive uses a t-interval over trace-family means and a logistic calibration intercept/slope instead of the ECE gate; reviewer uses a two-way ANOVA crossed variance instead of the larger of two one-way SEs. Decision vocabulary moved PASS/FAIL to ESTIMATED/NOT_ESTIMATED; committed empty fixtures still return NOT_ESTIMATED. 17 analyzer tests + 5 design-analysis tests pass; validate-specs and validate-artifacts pass. Closed the one residue Codex honestly flagged and correctly left out of its four-file scope: analysis-output.schema.json still enumerated PASS/FAIL for the statistical decision while the analyzer patched it in memory. Updated the on-disk enums to ESTIMATED/NOT_ESTIMATED (integrity-diagnostic PASS enums untouched) and neutered the in-memory shim to a pass-through. Synced the paper: §3.6 and the availability section previously called the reimplementation "a declared next step"; both now state that the specification and analyzer compute the estimators, which would otherwise have been an overclaim in reverse. Two design notes remain, from Codex, for a future pass: analysis-output could add a programme-level metrics object rather than repeating the grand estimate in each family record; and the crossed-variance estimator currently requires a balanced complete reviewer×case matrix, so an unbalanced target design needs an explicit extension. Build clean, 0 undefined citations, 39 pp, style clean. Repo now internally coherent across prose, design analysis, spec, schemas, analyzer, and tests.

2026-07-22 (Study B gate reframe) — Brett flagged that AP conditions Study B verdicts on interval lower bounds clearing fixed thresholds (0.20 margin, 0.90 correctness "each arm must separately reach"), the exact move the companion Delegation Assurance design analysis names a Type M error and reframes as design floors, "not thresholds an effect has to clear." DA is right, and I had preserved AP's gate language verbatim when moving it to the supplement while fixing the identical move in the judge section the same session. Read DA's treatment (sections-delegation/03-framework.tex:204 and scripts/design_analysis.py): its by-simulation design analysis finds a one-sided lower-bound gate exaggerates the effect it passes (Type-M exaggeration ~1.12 even at a zero gate; AP's 0.20 gate is stricter and worse), subtracting the larger of several nuisance shifts over-subtracts (~26% of the effect, because the max of noisy nonnegative quantities is biased up), a ten-bin ECE at 30 cases fails a calibrated predictor 97.6% of the time, and taking the larger of two one-way cluster SEs undercovers a crossed design (0.87). Reframed AP's Study B decision rule in Section 7a and the supplement: kept the numbers as design floors (smallest useful effect, target precision, design inputs), replaced the pass/fail threshold gates with a single multilevel estimand with partial pooling across families and bases reported with clustered uncertainty and tied to declared losses (consistent with the eight-schools reanalysis of the judge effect earlier this session), switched the margin from subtracting the larger nuisance shift to a modelled/mean shift, and cited reynolds2026delegationAssurance and gelman2014types. To prevent artifact-vs-paper drift, added revision notes to benchmark/study-b/design.md and projection-register.md flagging that the version-one gates in claim-register.json and scripts/analyze_study_b.py are superseded and pending re-derivation. NOT done (bigger follow-up, offered): re-deriving the machine-readable claim register and scripts/analyze_study_b.py to the multilevel form.

2026-07-22 (Study B analyzer re-derivation) — Re-derived scripts/analyze_study_b.py and benchmark/study-b/claim-register.json from the version-one threshold gates to the estimand form, so the machine-readable artifact now matches the paper and DA. Changes: (1) the selective margin subtracts the MEAN of the two absolute nuisance shifts, not the larger (the max of noisy nonnegative quantities is biased upward and over-subtracts). (2) The per-base one-sided lower-bound-above-0.20 gate is removed; the margin is reported as an estimand with a simultaneous interval, a 0.20 design floor, and a descriptive floor_position, never a PASS/FAIL. (3) The three-of-four conjunctive base gate is replaced by a family-level cluster estimand (mean base margin with a between-base Student-t interval; a design analysis shows the normal interval undercovers small clusters) and a cross-family DerSimonian-Laird random-effects pooled estimand (partial pooling supplies the multiplicity control the per-cell conjunction lacked). (4) behavioural_claim_gate (PASS/FAIL) becomes behavioural_claim, reporting the pooled estimand, scope, evaluability, and a decision_rule that ties any accept/revise/retire decision to declared losses, not to whether an interval clears the floor. (5) The main-reference, control, and shortcut checks are kept but documented as data-evaluability screens (did the arms produce interpretable reference behaviour), not effect gates; threshold_gate renamed evaluability_screen. The committed no-target record still returns NOT_ESTIMATED. Updated scripts/test_analyze_study_b.py to the new contract and scripts/validate_study_b_excellence.py, which had enforced the discredited "three of four"/"95%" gate tokens in the register's success_threshold, to require the estimand/design-floor/declared-losses framing instead and to forbid the old conjunctive gate. Reframed the register's declaration.tolerance (criterion, success_threshold, failure_threshold) to the estimand form. All of make validate-study-b passes (validate_study_b 28, excellence 10, analyzer 19) plus make assurance-check; paper and supplement rebuild clean. Design docs updated from "pending" to implemented. Pure-stdlib implementation (numpy not added to the analyzer); the t-quantile table and DerSimonian-Laird are closed-form. No reported result changes, since no target data exists.

2026-07-23 (action-vocabulary hook, from books/active/language-mediated-control) — Brett adopted a canonical action vocabulary for the book synthesizing these papers: EA's six-way taxonomy of actions under review (recommendation / proposal / attempted call / blocked call / completed call / downstream transaction; sections-evidentiary/05-sufficiency-test.tex:124) names what a review adjudicates; DA's three-way trace ontology (proposal record / gate decision / execution record) names the evidence layer. No doctrine change in either paper: both already hold that a gate success never launders a prohibited proposal (DA 03-framework.tex:79; EA 05-sufficiency-test.tex:138) and both make the verdict track the regime. **Hook for the DA agent:** DA's phrase "blocked before execution" (03-framework.tex:58) leaves implicit which operation counts as executed when a call is initiated and stopped mid-pipeline; EA makes that classification regime-relative (05-sufficiency-test.tex:128). One optional clarifying sentence at 03:58 would close the gap: where the proposal/execution boundary falls for a multi-stage operation is fixed by the regime's action classification, not by the gate's position in the pipeline. **Hook for the EA agent:** no change needed; awareness only, but the book now cites the six-way list as canonical, so renaming its members would ripple downstream.

2026-07-23 (C3 outcome and two hooks, from books/active/language-mediated-control) — The book's concordance item C3 (four field inventories for the projection declaration) was adjudicated on close reading as role-indexing, not rivalry, and the fleet claim that the AGI paper lacks a minimum-reach/withdrawal condition was refuted against the primary text (its declaration field 1 carries minimum reach; field 10 plus the following paragraph carry fallback and retirement). Brett adopted a core-plus-profiles canonical schema as a book artifact: draft at books/active/language-mediated-control/notes/projection-declaration-schema.md. No mandatory paper changes. **Hook for the DA agent (optional, one sentence):** the system-card list at 06-implications.tex:3 declares producer-side fields; a sentence noting that the deployer completes the risk measure and decision criterion, per the division of evidential labour, would align the card list with DA's own declared-losses decision layer (03-framework.tex:281). **Hook for the EA agent (referent question):** at 05-sufficiency-test.tex:147, "claim carrier" occupies the slot DA's card list calls "evaluation bearer" (the measured system), but the phrase reads as the warrant-carrying artifact; please confirm the intended referent at the next pass. The book schema currently reads it as the measured system. Full adjudication trail: books/active/language-mediated-control/notes/paper-concordance.md, item C2.

2026-07-23 — Tightened AP's intro against the "old hat" exposure the Claude Code security docs revealed (notes/claude-code-security-mapping.md): the outer assurance shell (provenance-binding, independent verification, non-compensatory verdicts, gating) is now standard first-party practice, so framing any of it as a contribution reads as stale. Two edits, both in Section 1, moving the principal claim onto the un-preempted core. (1) The principal-claim sentence no longer leads with "criterion-separated, projectibility-typed measurement"; it leads with measurement typed by linguistic status and by projectibility bearer, states that recording a result's configuration "isn't the hard part and isn't enough," and names the typed decomposition into four independently-failing bearers as the claim. This distinguishes the paper from deployed tooling (e.g. a revision stamp) that records config-relativity but doesn't decompose it. (2) The projectibility paragraph now says explicitly that the point isn't that a result is configuration-relative (a provenance record captures that) but that one record can't say which bearer a change moved. Left unchanged: the abstract (already led by "linguistically controlled taxonomy" and the four-inference-targets sentence, the un-preempted core) and the judge material (already empirical demonstration — the minority-class collapse, item-clustered intervals, pooling — not a "validate your judge" injunction). Rationale also follows the LLM-staleness law: weight the paper toward the typed conceptual core, which ages slowest, since the practice layer will be more standard by review time. Build clean at 32 pages.
